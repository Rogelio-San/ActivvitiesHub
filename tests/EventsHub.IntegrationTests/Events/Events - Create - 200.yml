info:
  name: Events - Create - 200
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/events"
  body:
    type: json
    data: |-
      {
        "id": "{{$guid}}",
        "title": "{{$randomCatchPhrase}}",
        "date": "{{$randomDateFuture}}",
        "description": "{{$randomLoremParagraph}}",
        "category": "culture",
        "isCancelled": false,
        "city": "{{$randomCity}}",
        "venue": "{{$randomStreetAddress}}",
        "latitude": "20.6843",
        "longitude": "-88.5678"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Status code is 404 Not Found", function () {
          
          expect(res.getStatus()).to.equal(404);
        });

        test("Response returns an array", function() {
          
          const body = res.getBody();
          expect(body).to.equal("The event was not found");  
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
